<Page x:Class="StroyMat.MaterialListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StroyMat"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MaterialListPage">

    <Grid>
        <Grid.RowDefinitions>
           
            <RowDefinition Height="90"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Margin="7,0">Поиск</TextBlock>
                <TextBox Name="TbSearch" Width="150" Height="30" Margin="7,0" TextChanged="TbSearch_TextChanged"></TextBox>
                <Button Name="ButtAdd" Height="30" Width="110" Margin="0,5" Click="ButtAdd_Click">Добавить</Button>
            </StackPanel>
            <TextBlock Margin="2,25" Name="TbCount">Записей:</TextBlock>
            <StackPanel>
                <TextBlock HorizontalAlignment="Center">Сортировка</TextBlock>
                <ComboBox Name="CbSort" Height="30" Width="110" Margin="7,0" SelectionChanged="CbSort_SelectionChanged">
                    <ComboBoxItem>Название возр.</ComboBoxItem>
                    <ComboBoxItem>Название убыв.</ComboBoxItem>
                    <ComboBoxItem>Стоимость возр.</ComboBoxItem>
                    <ComboBoxItem>Стоимость убыв.</ComboBoxItem>
                    <ComboBoxItem>Остаток возр.</ComboBoxItem>
                    <ComboBoxItem>Остаток убыв.</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel>
                <TextBlock HorizontalAlignment="Center">Фильтрация</TextBlock>
                <ComboBox Name="CbFilt" Height="30" Width="110" Margin="7,0" SelectionChanged="CbFilt_SelectionChanged"></ComboBox>
            </StackPanel>
            <Button Name="ButtEditMin" Width="150" Height="30" Margin="7,0,0,19" Click="ButtEditMin_Click" FontSize="8" Visibility="Hidden">Изменить минимальное кол-во</Button>
        </StackPanel>
        <ListView Name="LVMaterial" Grid.Row="1" SelectionMode="Extended" SelectionChanged="LVMaterial_SelectionChanged">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center" Orientation="Vertical"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border Background="{Binding MinColor}">
                            <StackPanel Orientation="Horizontal">
                                <Image Width="100" Height="100" Source="{Binding image}" Margin="7,0" VerticalAlignment="Center"></Image>
                                <StackPanel Margin="0,15">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Name="TbMaterialType" Text="{Binding MaterialType.Title}"></TextBlock>
                                        <TextBlock Margin="7,0">|</TextBlock>
                                        <TextBlock Name="TbMaterialTitle" Text="{Binding Title}"></TextBlock>
                                    </StackPanel>
                                    <TextBlock Name="TbMinCount" Text="{Binding MinCount, StringFormat={}Минимальное количество: {0} шт}"></TextBlock>
                                    <TextBlock Width="450" TextWrapping="Wrap" Name="TbSupplier" Loaded="TbSupplier_Loaded" Uid="{Binding ID}"></TextBlock>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Name="TbLeft" Text="{Binding CountInStock, StringFormat={}Остаток: {0} шт}"></TextBlock>
                                    <Button Name="ButtEdit" Uid="{Binding ID}" Click="ButtEdit_Click">Редактировать</Button>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="2">
            <TextBlock Name="txtPrev" Uid="prev" Text=" Пред. " MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txt1" Uid="1" Text="{Binding  Path = NPage[0], StringFormat={}{0,3}}" Visibility="{Binding Visible[0]}" FontWeight="{Binding Bold[0]}" MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txt2" Uid="2" Text="{Binding  Path = NPage[1], StringFormat={}{0,3}}" Visibility="{Binding Visible[1]}" FontWeight="{Binding Bold[1]}" MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txt3" Uid="3" Text="{Binding  Path = NPage[2], StringFormat={}{0,3}}" Visibility="{Binding Visible[2]}" FontWeight="{Binding Bold[2]}" MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txt4" Uid="4" Text="{Binding  Path = NPage[3], StringFormat={}{0,3}}" Visibility="{Binding Visible[3]}" FontWeight="{Binding Bold[3]}" MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txt5" Uid="5" Text="{Binding  Path = NPage[4], StringFormat={}{0,3}}" Visibility="{Binding Visible[4]}" FontWeight="{Binding Bold[4]}" MouseDown="GoPage_MouseDown"/>
            <TextBlock Name="txtNext" Uid="next" Text=" След." MouseDown="GoPage_MouseDown"/>
        </StackPanel>
    </Grid>
</Page>
